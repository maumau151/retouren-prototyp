
  # Mobile Prototype

  This is a code bundle for Mobile Prototype. The original project is available at https://www.figma.com/design/4Nu0n191rEKt5Z7D5tIda7/Mobile-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  